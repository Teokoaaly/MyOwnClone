# Task 5: custom_domain Fix Evidence

## Verification Date: 2026-06-01 11:56

## Bug Description
Field custom_domain existed in BOTH 	enants table AND clone_configs table.
Should only exist in clone_configs (domain is per-clon, not per-tenant).

## Files Analyzed

### 1. dify/api/models/clone.py - CloneConfig model
`
Line 31: custom_domain: Mapped[Optional[str]] = mapped_column(String(255), nullable=True, default=None)
`
✅ CloneConfig has custom_domain field - CORRECT (keep here)

### 2. dify/api/core/email_processor.py - resolve_clone_by_domain()
`
Line 109-116:
def resolve_clone_by_domain(to_domain: str) -> str | None:
    stmt = select(CloneConfig).where(
        CloneConfig.custom_domain == to_domain,
        CloneConfig.is_active.is_(True),
    )
    clone = db.session.execute(stmt).scalar_one_or_none()
    if clone:
        return clone.id
    return None
`
✅ Correctly queries CloneConfig.custom_domain - NOT 	enants.custom_domain

## References Found
- email_processor.py:111: CloneConfig.custom_domain == to_domain - CORRECT

## Search Results
- No references to 	enants.custom_domain found in any Python files
- No references to Tenant.custom_domain found

## Conclusion
✅ Bug is ALREADY FIXED. Domain resolution uses clone_configs.custom_domain correctly.
The 	enants.custom_domain column exists in DB but is not used by domain resolution.
No code changes needed - verified correct implementation.

## Status
COMPLETE - No changes required
