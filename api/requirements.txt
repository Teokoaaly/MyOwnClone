Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.0.0
Flask-CORS>=4.0.0
Flask-Babel>=3.1.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
redis>=5.0.0
weaviate-client>=4.4.0
flask-restx>=1.3.0
pydantic>=2.5.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
stripe>=5.0.0
PyJWT>=2.8.0
bcrypt>=4.1.0

# LLM provider SDKs (required by core.model_manager at import-time
# for each respective provider)
openai>=1.30.0
anthropic>=0.30.0

# Local speech-to-text (in-process faster-whisper / CTranslate2).
# Optional at install-time: when missing the registry falls back to
# cloud STT providers. The api Docker image installs it eagerly so
# VPS deployments get STT out of the box without external API keys.
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# T2.1: ingestion RAG (extracción HTML de URLs)
requests>=2.31.0
beautifulsoup4>=4.12.0
cryptography>=42.0.0  # para SecretCipher (cifrado API keys de modelos IA)

# pgvector para búsqueda semántica (T1.4)
pgvector>=0.3.0

# T2.3: extracción de PDFs
PyPDF2>=3.0.0

# T2.4: extracción de transcripciones de YouTube
youtube-transcript-api>=0.6.0

# T2.12: Sentry error tracking (opcional, solo si SENTRY_DSN está configurado)
sentry-sdk[flask]>=2.0.0

# T3.5: Worker asincrono para ingestion de PDFs grandes
rq>=1.16.0

# T3.6: Prometheus metrics
prometheus-client>=0.20.0
